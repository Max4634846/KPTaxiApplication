<Window x:Class="KPTaxiApplication.Views.UsersPanelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KPTaxiApplication.Views" 
        xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
        mc:Ignorable="d"
        Title="AdminPanelWindow" Height="850" Width="1250"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
        <Border Background="#eff2f7" CornerRadius="30" MouseDown="Border_MouseDown" MouseLeftButtonDown="Border_LeftMouseDown">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="200" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock  Margin="20 28 0 35" Visibility="Hidden" />

                    <Border Grid.Row="1" Background="#816aba" CornerRadius="0 50 0 30" Visibility="{Binding IsBorderVisible1}" x:Name="panelMenu">
                        <DockPanel>
                            <Border Width="80" Height="80" Padding="3" Background="White" CornerRadius="100"
                            DockPanel.Dock="Top" HorizontalAlignment="Left" Margin="20 -30 0 0">
                                <Ellipse Width="70" Height="70">
                                    <Ellipse.Fill>
                                        <ImageBrush  Stretch="UniformToFill" x:Name="imageUser" ImageSource="/images/iconAdmin2.png"/>
                                    </Ellipse.Fill>
                                </Ellipse>
                            </Border>
                            <TextBlock x:Name="txtStatus" Text="Админ" Foreground="White" FontSize="22" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Margin="5 0 0 10" Style="{StaticResource GlowingTextBlock}"/>

                        <TextBlock x:Name="txtUserName" Foreground="White" FontSize="16" FontWeight="SemiBold"
                                DockPanel.Dock="Top" TextAlignment="Center" Margin="5 0 0 10" Style="{StaticResource GlowingTextBlock}"/>


                        <TextBlock x:Name="txtOpicanie" Foreground="White" FontSize="14" FontWeight="SemiBold"
                               DockPanel.Dock="Top" TextAlignment="Center" Style="{StaticResource GlowingTextBlock}">
                                <Run>Приложение управляется</Run>
                                <LineBreak/>
                                <Run>администратором</Run>
                            </TextBlock>

                            <Separator Margin="10" DockPanel.Dock="Top" Height="0.8" Background="#816aba" x:Name="separator"/>

                            <StackPanel DockPanel.Dock="Top">

                                <Button Style="{StaticResource menuButton}" Click="Glav_Click" x:Name="passAc">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="Account" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Личный кабинет" />
                                    </StackPanel>
                                </Button>

                                <Button Style="{StaticResource menuButton}" Click="Home_Click" x:Name="home">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="Post" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Заказы" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Sot_Click" x:Name="sot">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="TrayPlus" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Сотрудники" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Tarig_Click" x:Name="taff">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="Table" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Тарифы" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Client_Click" x:Name="client">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="ClipboardAccount" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Клиенты" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Post_Click" x:Name="dol">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="FaceAgent" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Должность" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Model_Click" x:Name="mod">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="CarArrowRight" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Модель" />
                                    </StackPanel>
                                </Button>

                            <Button Style="{StaticResource menuButton}" Click="Car_Click" x:Name="avt">
                                    <StackPanel Orientation="Horizontal">
                                        <Icon:PackIconMaterial Kind="Car" Style="{StaticResource menuButtonIcon}"/>
                                        <TextBlock Text="Автомобили" />
                                    </StackPanel>
                                </Button>


                            </StackPanel>
                        <Button Style="{StaticResource menuButton}" VerticalAlignment="Bottom" DockPanel.Dock="Bottom" Margin="15 0 15 20" Click="Close_Click" x:Name="exit"> 
                                <StackPanel Orientation="Horizontal">
                                    <Icon:PackIconMaterial Kind="Logout" Style="{StaticResource menuButtonIcon}"/>
                                    <TextBlock Text="Выход" />
                                </StackPanel>
                            </Button>

                        </DockPanel>
                    </Border>
                </Grid>
                <Border x:Name="Time" Grid.Row="0" Grid.Column="1" Background="#816aba" Width="350" Height="45" VerticalAlignment="Top"  HorizontalAlignment="Center" CornerRadius="0 0 30 30" Panel.ZIndex="1">
                    <TextBlock x:Name="txtTime" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="AliceBlue" Style="{StaticResource GlowingTextBlock}" />
                </Border>
                <Frame Grid.Column="1" x:Name="MainFrame" NavigationUIVisibility="Hidden" />
            </Grid>
        </Border>
    
</Window>
