<Page x:Class="KPTaxiApplication.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KPTaxiApplication.Views" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      Height="800" Width="800"
      Title="LoginPage">

    <materialDesign:Card UniformCornerRadius="35" Margin="150, 100" Opacity="0.95">
    <materialDesign:Card.Background>
        <SolidColorBrush Color="#fcfcfc" />
    </materialDesign:Card.Background>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Image Grid.Row="0"  Source="/image/txIcon.jpeg"  Width="150" Stretch="UniformToFill"/>
        <TextBlock Grid.Row="1" Text="Авторизация" FontSize="28" HorizontalAlignment="Center"/>
        <TextBlock Grid.Row="1" Text="Ваше личное такси" FontSize="16" HorizontalAlignment="Center" Margin="0,45,0,0"/>
        <WrapPanel  Grid.Row="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="/image/UserIcon.png" Width="35" Margin="5"></Image>
            <TextBox Width="350" x:Name="txtLogin" Style="{StaticResource TextBoxStyle}">
            </TextBox>
        </WrapPanel>
        <WrapPanel Grid.Row="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Image Source="/image/PasswordIcon.png" Width="32" Margin="5"></Image>
            <PasswordBox x:Name="passwordBox" Width="350" Style="{StaticResource PasswordBoxStyle}" 
                PasswordChanged="PasswordBox_PasswordChanged" />

        </WrapPanel>
        <TextBlock Grid.Row="4" VerticalAlignment="Center" HorizontalAlignment="Center" Text="{Binding ErrorMessage}" Style="{StaticResource ErrorMessgaeStyle}"/>
        <Button Grid.Row="5"  Width="250" Height="35" Content="Войти" Style="{StaticResource SecondaryButton}" Click="Login_Click"></Button>
        <Button Visibility="Collapsed" Grid.Row="6"  Width="115" Height="25" Content="Зарегистрироваться" Foreground="#E776F9" BorderThickness="0" VerticalAlignment="Top">
            <Button.Background>
                <SolidColorBrush Color="Transparent"/>
            </Button.Background>
        </Button>
        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" />
    </Grid>

    </materialDesign:Card>
</Page>
